"use client";

import DevToolCard from "./DevToolCard";

export default function RegexTesterTool() {
  return (
    <DevToolCard
      slug="regex-tester"
      title="Regex Tester"
      badge="Patterns"
      description="Test JavaScript regular expressions against sample text and inspect captured groups."
    />
  );
}

