"use client";

import DevToolCard from "./DevToolCard";

export default function UuidGeneratorTool() {
  return (
    <DevToolCard
      slug="uuid-generator"
      title="UUID Generator"
      badge="One‑click"
      description="Generate RFC 4122 UUID v4 values for identifiers, fixtures, and test data."
    />
  );
}

