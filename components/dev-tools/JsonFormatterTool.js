"use client";

import DevToolCard from "./DevToolCard";

export default function JsonFormatterTool() {
  return (
    <DevToolCard
      slug="json-formatter"
      title="JSON Formatter"
      badge="Live preview"
      description="Format, validate, and pretty‑print JSON responses and payloads."
    />
  );
}

