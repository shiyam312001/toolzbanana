"use client";

import DevToolCard from "./DevToolCard";

export default function HtmlBeautifierTool() {
  return (
    <DevToolCard
      slug="html-beautifier"
      title="HTML Beautifier"
      badge="Clean markup"
      description="Tidy up HTML snippets with consistent indentation and structure for easier reading."
    />
  );
}

