# Public site URL (used for canonical URLs, sitemap, Open Graph)
NEXT_PUBLIC_SITE_URL=https://toolzbanana.vercel.app

# remove.bg API key (server-only — never expose as NEXT_PUBLIC_*)
REMOVE_BG_API_KEY=

# Optional: AdSense ad unit slot ID for leaderboard placements
NEXT_PUBLIC_ADSENSE_SLOT_LEADERBOARD=

# Contact form (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
CONTACT_TO_EMAIL=
